# Gyro

Cross-platform asynchronous event loop and I/O runtime, built on epoll, kqueue and IOCP.
Implemented in C++17 behind a C API, so it can be embedded in language runtimes and applications that need non-blocking I/O.

> Early development. Nothing is stable yet.
